<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        THE LAUNCHER, ANDROID 12+: THE SYSTEM SPLASH IN HOME'S COLOUR.
        Android 12 draws a splash for every cold start from these attributes. Android skips the
        starting window for a translucent activity, so this version is opaque, and its window
        is the same #f3faf6 as the system splash, the TWA splash and HOME: the hand-over from one
        to the next never flashes another colour.
    -->
    <style name="Theme.Epinoia.Launcher" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/epinoia_ground_light</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowSplashScreenBackground">@color/epinoia_ground_light</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/epinoia_ground_light</item>
    </style>

    <style name="Theme.Epinoia.Settings" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/epinoia_ground_light</item>
        <item name="android:statusBarColor">@color/epinoia_ground_light</item>
        <item name="android:navigationBarColor">@color/epinoia_ground_light</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:colorAccent">@color/epinoia_lume_light</item>
    </style>
</resources>
