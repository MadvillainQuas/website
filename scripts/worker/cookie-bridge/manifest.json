{
  "manifest_version": 3,
  "name": "Epinoia cookie bridge",
  "version": "1.0",
  "description": "Hands the AI worker on this PC your YouTube sign-in whenever it changes, so game downloads never wait on a cookie export.",
  "permissions": ["cookies", "alarms", "storage"],
  "host_permissions": ["*://*.youtube.com/*", "http://127.0.0.1/*"],
  "background": { "service_worker": "background.js" },
  "action": { "default_title": "Epinoia cookie bridge — click to send the YouTube sign-in to the worker now" },
  "icons": {}
}
